# smithery.yaml

startCommand:
  type: stdio
  command: python
  args:
    - mcp_stdio_proxy.py

# Adding an empty config schema prevents Smithery from throwing a deployment warning.
# You can expand this later if you require users to input an API key for a Pro tier.
configSchema:
  type: object
  properties: {}